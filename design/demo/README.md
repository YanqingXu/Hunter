# 历史首版回归内容

`首版.xlsx` 只作为旧地图 2 单人撤离闭环的冻结回归夹具，由 `export/demo.py` 读取。
当前生产入口为根目录 `design/demo_sources.json` 和 `export/gameplay.py`，不再依赖此工作簿。
正式源表与所选记录见 [DEMO 内容说明](../DEMO内容说明.md)。

本工作簿取历史提交 `568e58b` 的地图 2 依赖闭包并补充 Bag 表、物品堆叠与 Boss 前后摇。
本轮已把地图、怪物招式和旧战利品迁入根目录生产闭包；此历史副本不再跟随生产调参。
`design/combat_demo.json` 只供灰盒回归测试，不参与正式服务内容生成。

首版：8 格背包、拾取半径 1500 mm；地图 2 的 Boss 出生实例 14 死亡后，
玩家回到出生地附近出口并停留 180 Tick。每局固定步枪和弹药，永久仓库只读。
