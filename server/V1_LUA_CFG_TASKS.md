# Lua 配置与玩法迁移任务

契约见 [SRV-013](intents/modules/cfg.intent.md)。基线提交 `332c1b4`，本轮 Windows 实现与验证
已完成，保留 SRV-012 历史验收记录。

| 切片 | 状态 | 验收 |
| --- | --- | --- |
| 独立 Lua 表、共享 Lua 配置解析校验 | 已验证 | 19 张 Lua 表；确定性、中文转义、非法引用／缺值／单位、草稿隔离、失败回滚 |
| 数据模块装配与源码／Bundle 加载 | 已验证 | 同内容身份；同一程序只更新脚本即可将移动速度从 100 改为 37 mm/Tick |
| Lua 配装、初始参数、物品与工具规则 | 已验证 | 配装先验后分配局号、拾取叠堆、槽满原子拒绝、工具耗尽、补给幂等、免费物资不入库 |
| Host／上下文／状态 v7 | 已验证 | 旧版本拒绝；候选失败不修改活动状态／句柄；持久身份整数边界 |
| 双 Runtime 与交付 | 已验证 | 开发 32/32、Bundle 31/31；连续十局、十次启停、旧档案、强杀恢复、生产链接和两种解包联调通过 |

Python 只处理工作簿、字段类型、记录选择及确定性序列化；`game.cfg` 负责转换、关联、默认配装
和玩法语义。共享 JSON 与 `ContentId.h` 均为 Lua 输出的派生内容，服务端仅从源码或签名 Bundle
加载 Lua。C++ 保留权威状态、内容身份、结构边界和有界原子接口，不缓存完整配置。

正式入口仍为 `design/demo_sources.json`；策划 EXE 已重新打包，草稿输出隔离到
`server/build/draft-cfg`。原生 Host／上下文／内部状态为 v7，网络 v5、内容 v4、SQLite V1 不变。
正式内容摘要保持 `gameplay-v4:65104619225ac997076578c36c5d4cca91ebf28af6fe7d66df19f13d1b056908`。

两种联调包已经独立解压，完成真实 TCP 战斗、拾取、撤离、Committed、重启查询；逐文件长度、
SHA-256 和 ZIP CRC 均已核对。Bundle 使用公开测试向量签名，仅供联调，不作为正式发行签名。
Unity 联调、Android NDK／真机和正式发行仍独立验收；没有增加热更新或未结算对局崩溃续玩。

最终完整回归用时：开发 315.77 秒、Bundle 304.84 秒；实际证据和交付摘要见
[VERIFICATION.md](VERIFICATION.md)。
